#################################
#Notifies me via telegram when  #
#an update for HASS is available#
#################################

alias: 'Update Available Notifications'
trigger:
  platform: state
  entity_id: updater.updater
action:
  service: notify.telegram_marc
  data:
    message: 'Update for Home Assistant is available.'