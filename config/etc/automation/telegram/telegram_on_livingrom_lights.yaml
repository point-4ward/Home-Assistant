###########################################
#This automation switches on the living   #
#room lights when instructed via telegram #
###########################################

alias: 'Telegram set livingroom lights on'
trigger:
  platform: event
  event_type: telegram_command
  event_data:
    command: '/onLivingRoom'
action:
  - service: homeassistant.turn_on
    entity_id: light.living_room
  - service: telegram_bot.send_message
    data_template:
      target: '{{ trigger.event.data.user_id }}'
      message: 'Livingroom lights have been turned on'