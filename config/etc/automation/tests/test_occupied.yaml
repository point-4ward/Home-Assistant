##########################
#Notification to test the#
#reliability of a sensor #
##########################

alias: 'Test bluetooth tracker Marc away'
initial_state: 'on'
trigger:
  platform: state
  entity_id: sensor.home_status
action:
  service: notify.telegram_marc
  data:
    message: 'The house is {{states.sensor.home_status.state}} .'