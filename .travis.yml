language: python
python:
  - "3.4"
before_install:
  - mv extras/travis_ci/secrets_redacted.yaml secrets.yaml
  - mv extras/travis_ci/travis_ssl_certificate travis_ssl_certificate
install:
  - pip3 install homeassistant
script:
  - hass -c . --script check_config
